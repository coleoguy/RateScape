% Generated by roxygen2 (manual): do not edit by hand
\name{summarizeRates}
\alias{summarizeRates}
\title{Summarize Rate Heterogeneity Model Results}
\usage{
summarizeRates(object, burnin_frac = 0, quantiles = c(0.025, 0.975), ...)
}
\arguments{
\item{object}{An object of class "ratescapeFit" or "ratescapeML" (from
\code{fitRateScape} or \code{rateCategories}).}

\item{burnin_frac}{Numeric in (0, 1). Fraction of posterior samples to exclude
as additional burn-in. Default is 0.}

\item{quantiles}{Numeric vector. Quantiles for credible intervals.
Default is c(0.025, 0.975) for 95\% CI.}

\item{...}{Additional arguments (currently unused).}
}
\value{
For Bayesian fits: a list containing:
\describe{
  \item{pi_summary}{Posterior summary of mixing proportion pi.}
  \item{sigma2_summary}{Posterior summary of heterogeneity variance sigma^2.}
  \item{rate_summary}{Per-branch posterior summaries of r_i.}
  \item{shifted_branches}{Branches with high probability of rate shift.}
  \item{bayes_factor}{Bayes factor for heterogeneity.}
  \item{acceptance_rate}{MH acceptance rate.}
}

For ML fits: a list containing:
\describe{
  \item{best_fit_summary}{Parameters of best-fit model.}
  \item{bic_table}{Comparison of BIC across k values.}
  \item{rate_estimates}{Maximum likelihood estimates of branch rates.}
}
}
\description{
Produces posterior summaries of estimated parameters, computes Bayes factors
and information criteria, and generates comparison tables between different
model fits.
}
\details{
For Bayesian fits (ratescapeFit): computes posterior mean, SD, and credible
intervals for r_i, pi, sigma^2. Computes Bayes factor for heterogeneity
vs. null (no heterogeneity). Reports acceptance rates for diagnostic
checking and identifies branches with high posterior probability of rate shifts.

For ML fits (ratescapeML): reports maximum likelihood estimates and standard
errors; compares BIC across tested k values; presents estimated gamma shape
and rate parameters.

Bayes factor interpretation: BF > 3 = weak evidence; BF > 10 = strong evidence
for rate heterogeneity.
}
\examples{
\dontrun{
  fit <- fitRateScape(tree, data, Q, lambda_sigma = 1.0)
  summ <- summarizeRates(fit)
  print(summ)

  fit_ml <- rateCategories(tree, data, Q)
  summarizeRates(fit_ml)
}
}
\seealso{
\code{\link{fitRateScape}}, \code{\link{rateCategories}}, \code{\link{plotRateTree}}
}
