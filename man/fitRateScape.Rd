% Generated by roxygen2 (manual): do not edit by hand
\name{fitRateScape}
\alias{fitRateScape}
\title{Bayesian Spike-and-Slab Fitting for Rate Heterogeneity}
\usage{
fitRateScape(
  tree,
  data,
  Q = NULL,
  estimate_Q = FALSE,
  lambda_sigma,
  expected_background = NULL,
  root_prior = "fitzjohn",
  q_prior = "diffuse",
  ngen = 10000,
  burn_in = 2000,
  thin = 10,
  tau_init = 0.3,
  target_acceptance = 0.3,
  seed = NULL
)
}
\arguments{
\item{tree}{phylo object.}

\item{data}{Data frame with tip states. Row names must match tree tip labels.
The first column should contain numeric state values (0-indexed).}

\item{Q}{Rate matrix (k x k). If NULL, an Mk model is used.}

\item{estimate_Q}{Logical. Co-estimate Q during MCMC? Default FALSE.}

\item{lambda_sigma}{Numeric. Rate parameter for exponential prior on sigma^2. Required (no default).}

\item{expected_background}{Numeric in [0, 1] or NULL. Expected proportion of
background-rate branches. If NULL, a uniform prior on pi is used.}

\item{root_prior}{Character. Root state prior: "fitzjohn", "equal", or "stationary".}

\item{q_prior}{Character. Prior on Q elements: "diffuse", "moderate", "informative", or numeric.}

\item{ngen}{Integer. Number of MCMC generations (post-burn-in). Default 10000.}

\item{burn_in}{Integer. Number of burn-in iterations. Default 2000.}

\item{thin}{Integer. Thinning interval for posterior samples. Default 10.}

\item{tau_init}{Numeric. Initial Metropolis-Hastings proposal standard deviation. Default 0.3.}

\item{target_acceptance}{Numeric. Target MH acceptance rate for adaptive tuning during burn-in. Default 0.30.}

\item{seed}{Integer or NULL. Random seed for reproducibility.}
}
\value{
An object of class \code{"ratescapeFit"} containing:
\describe{
  \item{mcmc_samples}{List with posterior samples of r (rates), z (indicators), pi, sigma2, loglik.}
  \item{tree}{The input phylogenetic tree.}
  \item{data}{Numeric vector of tip states used.}
  \item{Q_fixed}{The Q matrix used.}
  \item{acceptance_rate}{Metropolis-Hastings acceptance rate.}
  \item{rj_acceptance_rate}{Reversible-jump acceptance rate.}
  \item{tau_final}{Final tuned proposal SD.}
  \item{bayes_factor}{Bayes factor for heterogeneity.}
  \item{prior_settings}{List of prior hyperparameters used.}
}
}
\description{
Fits a spike-and-slab mixture model for branch-specific rate heterogeneity
in discrete character evolution using reversible-jump MCMC. Each branch is
assigned a rate scalar r_i: background branches have r_i = 1 (spike), while
rate-shifted branches have r_i drawn from a LogNormal slab distribution.
}
\details{
The model uses a joint (z_i, r_i) reversible-jump MCMC with incremental
cache updates for efficient likelihood computation. During burn-in, the
proposal standard deviation (tau) is adaptively tuned to achieve the target
acceptance rate.

The spike-and-slab prior is:
\itemize{
  \item z_i ~ Bernoulli(pi): indicator for background (z=1) vs shifted (z=0)
  \item r_i = 1 if z_i = 1 (spike / background)
  \item r_i ~ LogNormal(0, sigma^2) if z_i = 0 (slab / shifted)
  \item sigma^2 ~ Exp(lambda_sigma)
  \item pi ~ Beta(a, b) parameterized by expected_background
}
}
\examples{
\dontrun{
  library(ape)
  tree <- rcoal(50)
  Q <- makeQ(model = "mk", k = 2)
  sim <- simRateScape(tree, Q, lambda_sigma = 1.0, nrep = 1)
  dat <- data.frame(state = sim$data[1,], row.names = tree$tip.label)
  fit <- fitRateScape(tree, dat, Q, lambda_sigma = 1.0, ngen = 5000)
  summary(fit)
  plot(fit)
}
}
\seealso{
\code{\link{simRateScape}}, \code{\link{summarizeRates}}, \code{\link{plotRateTree}}, \code{\link{checkPrior}}
}
