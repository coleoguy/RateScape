% Generated by roxygen2 (manual): do not edit by hand
\name{compareModels}
\alias{compareModels}
\title{Compare Multiple Model Fits Side by Side}
\description{
When you have run several analyses with different settings — for example,
different prior specifications, different Q matrices, or Bayesian vs. ML
approaches — this function produces a summary table comparing them all.
This helps you decide which model best describes your data and assess how
sensitive your conclusions are to modeling choices.

Common comparisons include:
\itemize{
  \item Different \code{lambda_sigma} values (prior sensitivity analysis).
  \item With vs. without phylogenetic smoothing.
  \item With vs. without covariates.
  \item Bayesian (\code{\link{fitRateScape}}) vs. ML
    (\code{\link{rateCategories}}).
  \item Different Q matrix models (Mk vs. ARD).
}
}
\usage{
compareModels(..., names = NULL)
}
\arguments{
\item{...}{Two or more fitted model objects. These can be any mix of
\code{"ratescapeFit"} (from \code{\link{fitRateScape}}) and
\code{"ratescapeML"} (from \code{\link{rateCategories}}).}

\item{names}{Optional character vector of labels for each model.
If \code{NULL}, labels are generated automatically ("Model 1", "Model 2",
etc.). Providing descriptive names makes the output much more readable.}
}
\value{
A data frame with one row per model and columns for comparison metrics.
For Bayesian fits these include Bayes factors, posterior mean pi and
sigma^2, number of shifted branches, and acceptance rates. For ML fits,
BIC and number of rate categories are reported.
}
\examples{
\dontrun{
  Q <- makeQ(model = "mk", k = 2)

  # Fit with different prior strengths
  fit_diffuse <- fitRateScape(tree, data, Q, lambda_sigma = 0.5,
                               ngen = 50000)
  fit_moderate <- fitRateScape(tree, data, Q, lambda_sigma = 1.0,
                                ngen = 50000)
  fit_informative <- fitRateScape(tree, data, Q, lambda_sigma = 5.0,
                                   ngen = 50000)

  # Compare them
  comp <- compareModels(fit_diffuse, fit_moderate, fit_informative,
                         names = c("diffuse", "moderate", "informative"))
  print(comp)
  # If results are similar across prior settings, your conclusions are
  # robust. If they differ substantially, report the sensitivity.

  # Compare Bayesian vs ML
  fit_ml <- rateCategories(tree, data, Q)
  comp2 <- compareModels(fit_moderate, fit_ml,
                          names = c("Bayesian", "ML"))
  print(comp2)

  # Compare with and without smoothing
  fit_smooth <- fitRateScape(tree, data, Q, lambda_sigma = 1.0,
                              smoothing = 2.0, ngen = 50000)
  comp3 <- compareModels(fit_moderate, fit_smooth,
                          names = c("No smoothing", "Smoothing = 2"))
}
}
\seealso{
\code{\link{fitRateScape}}, \code{\link{rateCategories}},
\code{\link{summarizeRates}} (detailed summaries of individual fits)
}
