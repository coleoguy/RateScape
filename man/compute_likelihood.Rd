% Generated by roxygen2 (manual): do not edit by hand
\name{compute_likelihood}
\alias{compute_likelihood}
\title{Compute Likelihood of Discrete Character Data Under Rate Scalars}
\usage{
compute_likelihood(tree, data, Q, r_scalars, root_prior = "fitzjohn",
  log_scale = TRUE)
}
\arguments{
\item{tree}{An object of class "phylo".}

\item{data}{Numeric vector of character states at the tips (0-indexed).}

\item{Q}{Transition rate matrix (k x k).}

\item{r_scalars}{Numeric vector of rate multipliers (one per edge).}

\item{root_prior}{Character: "fitzjohn", "equal", or "stationary". Default is "fitzjohn".}

\item{log_scale}{Logical. If TRUE (default), return log-likelihood.}
}
\value{
Numeric scalar: the (log-)likelihood of the data given the tree, Q matrix,
and branch-specific rate scalars.
}
\description{
Implements the Felsenstein pruning algorithm with efficient partial-likelihood
caching for branch-specific rate scalar estimation.
}
\examples{
\dontrun{
  tree <- ape::rcoal(20)
  Q <- makeQ(model = "mk", k = 2)
  states <- sample(0:1, 20, replace = TRUE)
  r <- rep(1.0, nrow(tree$edge))
  ll <- compute_likelihood(tree, states, Q, r)
}
}
\seealso{
\code{\link{fitRateScape}}, \code{\link{rateCategories}}
}
