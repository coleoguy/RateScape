% Generated by roxygen2 (manual): do not edit by hand
\name{rateCategories}
\alias{rateCategories}
\title{Maximum Likelihood Discretized Gamma Rate Categories}
\usage{
rateCategories(
  tree,
  data,
  Q,
  k_min = 2,
  k_max = 8,
  em_tol = 1e-06,
  em_maxiter = 100,
  root_prior = "fitzjohn",
  seed = NULL
)
}
\arguments{
\item{tree}{An object of class "phylo" (ape package).}

\item{data}{A data frame with rows as species and a single column of discrete states.
Row names must match tree tip labels.}

\item{Q}{A transition rate matrix (k x k).}

\item{k_min}{Integer. Minimum number of rate categories. Default is 2.}

\item{k_max}{Integer. Maximum number of rate categories. Default is 8.}

\item{em_tol}{Numeric. EM convergence tolerance. Default is 1e-6.}

\item{em_maxiter}{Integer. Maximum EM iterations. Default is 100.}

\item{root_prior}{Character. Root state prior: "fitzjohn", "equal", or "stationary". Default is "fitzjohn".}

\item{seed}{Integer or NULL. Random seed.}
}
\value{
An object of class "ratescapeML" containing:
\describe{
  \item{tree}{The input tree.}
  \item{best_fit}{Best-fit model results including rates and category weights.}
  \item{all_fits}{List of model fits for each k tested.}
  \item{bic_table}{BIC comparison across tested k values.}
  \item{best_k}{Optimal number of rate categories selected by BIC.}
}
}
\description{
Fits a discretized gamma model for rate heterogeneity using maximum likelihood
and the EM algorithm. Automatically selects the optimal number of rate categories
via BIC model selection.
}
\details{
This function provides a maximum likelihood alternative to the Bayesian
spike-and-slab model in \code{fitRateScape}. It fits discretized gamma
rate categories (similar to the Yang 1994 approach for sequence data)
to discrete morphological characters.

For each number of categories k from \code{k_min} to \code{k_max}, the
EM algorithm iterates between assigning branches to rate categories (E-step)
and re-estimating category rates and weights (M-step). The optimal k is
selected by BIC.
}
\examples{
\dontrun{
  tree <- ape::rcoal(50)
  Q <- makeQ(model = "mk", k = 2)
  sim <- simRateScape(tree, Q, lambda_sigma = 1.0, nrep = 1)
  dat <- data.frame(state = sim$data[1,], row.names = tree$tip.label)
  fit_ml <- rateCategories(tree, dat, Q)
  summary(fit_ml)
  plot(fit_ml)
}
}
\seealso{
\code{\link{fitRateScape}}, \code{\link{summarizeRates}}, \code{\link{plotRateTree}}
}
