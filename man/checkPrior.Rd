% Generated by roxygen2 (manual): do not edit by hand
\name{checkPrior}
\alias{checkPrior}
\title{Prior Diagnostic Checking via Prior-Predictive Simulation}
\usage{
checkPrior(
  tree,
  Q,
  lambda_sigma,
  expected_background = NULL,
  nsim = 100
)
}
\arguments{
\item{tree}{An object of class "phylo" (ape package).}

\item{Q}{A transition rate matrix (k x k).}

\item{lambda_sigma}{Numeric. Rate parameter for the exponential prior on sigma^2.}

\item{expected_background}{Numeric in [0, 1] or NULL. Expected proportion of
background-rate branches. Default is NULL (uniform prior on pi).}

\item{nsim}{Integer. Number of prior-predictive simulations. Default is 100.}
}
\value{
An object of class "ratescape_prior_check" containing:
\describe{
  \item{prior_samples}{Samples from the prior distribution.}
  \item{prior_predictive_stats}{Summary statistics from prior-predictive simulations.}
  \item{overlap_measure}{Estimate of prior-posterior overlap (0-1).}
  \item{warnings}{Vector of warning messages if issues detected.}
  \item{call}{The function call.}
}
}
\description{
Assesses whether your prior specification is sensible for your data by
simulating datasets under the prior and comparing to your observed data.
}
\details{
This function performs prior-predictive simulation: it draws parameter values
from the prior (pi ~ Beta, sigma^2 ~ Exp(lambda_sigma), r_i ~ spike-and-slab
mixture) and simulates discrete character datasets under the model. Summary
statistics (e.g., number of state changes, entropy of state distribution) are
stored and compared against observed data patterns.

Visualization includes prior predictive distributions vs. observed data,
marginal prior distributions of pi and sigma^2, and diagnostic flags
for potential prior-data conflict.

Warning flags are raised if prior-posterior overlap is very high (>80\%),
suggesting the prior may dominate the inference, or if posterior estimates
are near prior boundaries.
}
\examples{
\dontrun{
  tree <- ape::rcoal(50)
  Q <- makeQ(model = "mk", k = 2)

  check <- checkPrior(tree, Q, lambda_sigma = 1.0,
                      expected_background = 0.80, nsim = 200)
  print(check)
  plot(check)
}
}
\seealso{
\code{\link{fitRateScape}}, \code{\link{simRateScape}}, \code{\link{makeQ}}
}
