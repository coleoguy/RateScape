% Generated by roxygen2 (manual): do not edit by hand
\name{makeQ}
\alias{makeQ}
\title{Construct Discrete Character Rate Matrices}
\usage{
makeQ(tree = NULL, model = "mk", k = NULL, Q = NULL, polyploid = TRUE)
}
\arguments{
\item{tree}{An object of class "phylo" (optional, used for determining number of states).}

\item{model}{Character. Type of rate matrix model: "mk", "sym", "ard", "chromosome", or "custom".}

\item{k}{Integer. Number of states. Required unless model = "custom" or tree is provided.}

\item{Q}{Matrix. User-specified transition matrix (required if model = "custom").}

\item{polyploid}{Logical. For chromosome model, whether to include polyploidy
transitions (doubling). Default is TRUE.}
}
\value{
A transition rate matrix (Q matrix) with dimensions k x k.
Off-diagonal entries are positive rates. Diagonal entries are negative,
set so that each row sums to 0.
}
\description{
Creates transition rate matrices (Q matrices) for discrete character evolution
models. Supports standard models (Mk, SYM, ARD) and specialized models for
chromosome number evolution and custom specifications.
}
\details{
\describe{
  \item{Mk Model}{All off-diagonal entries are equal (Q_ij = mu for i != j).
    Diagonal entries are set so row sums = 0.}
  \item{Symmetric (SYM) Model}{Forward and reverse transitions are equal (Q_ij = Q_ji).}
  \item{All-Rates-Different (ARD) Model}{Each off-diagonal entry is independent.}
  \item{Chromosome Model}{Specialized for chromosome number evolution:
    gain (i -> i+1), loss (i -> i-1), polyploid (i -> 2i).}
  \item{Custom}{User supplies the entire Q matrix.}
}
}
\examples{
\dontrun{
  # Mk model with 3 states
  Q_mk <- makeQ(model = "mk", k = 3)

  # ARD model with 4 states
  Q_ard <- makeQ(model = "ard", k = 4)

  # Chromosome model with 10 states
  Q_chr <- makeQ(model = "chromosome", k = 10, polyploid = TRUE)
}
}
\seealso{
\code{\link{fitRateScape}}, \code{\link{simRateScape}}
}
