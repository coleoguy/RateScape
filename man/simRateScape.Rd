% Generated by roxygen2 (manual): do not edit by hand
\name{simRateScape}
\alias{simRateScape}
\title{Simulate Discrete Character Data with Branch Rate Heterogeneity}
\usage{
simRateScape(
  tree,
  Q,
  lambda_sigma = 1,
  pi = 0.5,
  nrep = 1,
  r_scalars = NULL,
  root_state = NULL,
  return_rates = FALSE,
  seed = NULL
)
}
\arguments{
\item{tree}{An object of class "phylo".}

\item{Q}{Transition rate matrix (k x k).}

\item{lambda_sigma}{Numeric. Rate parameter for exponential prior on sigma^2.
Ignored if rates are provided directly via \code{r_scalars}.}

\item{pi}{Numeric in [0, 1]. Mixing proportion: probability that a branch
is at background rate (r = 1). Default is 0.5.}

\item{nrep}{Integer. Number of independent replicates to simulate. Default is 1.}

\item{r_scalars}{Numeric vector or NULL. Pre-specified branch rate scalars. If provided,
overrides the spike-and-slab prior. Length must equal number of edges. Default is NULL.}

\item{root_state}{Integer or NULL. Root character state (0 to k-1).
If NULL, sampled from the root prior.}

\item{return_rates}{Logical. If TRUE, also return the branch-specific rates.
Default is FALSE.}

\item{seed}{Integer or NULL. Random seed for reproducibility.}
}
\value{
A list containing:
\describe{
  \item{data}{Matrix of simulated character data (nrep rows x n_tips columns).}
  \item{rates}{(if return_rates = TRUE) List of branch rate scalars for each replicate.}
  \item{root_states}{Root states used for each replicate.}
  \item{tree}{The input tree.}
  \item{call}{The function call.}
}
}
\description{
Generates discrete character datasets under the RateScape spike-and-slab
or user-specified rate model. Useful for prior-predictive simulation,
validation, and simulation studies.
}
\details{
Simulation procedure:
\enumerate{
  \item If \code{r_scalars} is not provided, sample from the spike-and-slab prior:
    z_i ~ Bernoulli(pi); r_i = 1 if z_i = 1; r_i ~ LogNormal(0, sigma^2)
    if z_i = 0, where sigma^2 ~ Exp(lambda_sigma).
  \item If a \code{root_state} is not provided, sample from the root prior.
  \item Evolve the character down the tree using the Gillespie algorithm:
    at each branch with state s, the effective rate matrix is r_i * Q.
  \item Repeat for nrep independent simulations.
}
}
\examples{
\dontrun{
  tree <- ape::rcoal(50)
  Q <- makeQ(model = "mk", k = 2)

  # Simulate with default spike-and-slab prior
  sim1 <- simRateScape(tree, Q, lambda_sigma = 1.0, pi = 0.7, nrep = 10)

  # Simulate with fixed rates
  r_fixed <- rep(1, nrow(tree$edge))
  r_fixed[1:5] <- 2.0
  sim2 <- simRateScape(tree, Q, r_scalars = r_fixed, nrep = 1,
                       return_rates = TRUE)
}
}
\seealso{
\code{\link{fitRateScape}}, \code{\link{makeQ}}, \code{\link{checkPrior}}
}
